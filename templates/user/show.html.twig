{% extends 'backBase.html.twig' %}



{% block body %}
 <button class="btn btn-info btn-xs" style="position:absolute;top:80px;right:2%;"><a class="fa fa-home" href="{{ path('app_user_index') }}"> back to list
                   </a> </button>
    <br>
    <br>
<div class="col-lg-5">
          <div class="card">
            <div class="card-body">
              <h3 class="card-title">    <h3 align="center">Utilisateur</h3>
              <br>
			  <div class="table-responsive">
                <table class="table">
   
        <tbody align="center">
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.Username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                 <td>
                                               
                                            <img src="{{ asset('userpic/'~user.avatar) }}" alt="avatar"
                                                         style="width:100px ;margin-bottom:20%">
                            </td>
            </tr>
               <th>Role</th>
                <td>{{ user.roles }}</td>
            <tr>
                <th>IsVerified</th>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
                </div>
        </div>
      </div>
    <button class="btn"><a class="fa fa-pencil" href="{{ path('app_user_edit', {'id': user.id}) }}"> Edit</a> </button>
    {{ include('user/_delete_form.html.twig') }}


{% endblock %}
